# Copyright (c) 2010-2025, Lawrence Livermore National Security, LLC. Produced
# at the Lawrence Livermore National Laboratory. All Rights reserved. See files
# LICENSE and NOTICE for details. LLNL-CODE-806117.
#
# This file is part of the MFEM library. For more information and source code
# availability visit https://mfem.org.
#
# MFEM is free software; you can redistribute it and/or modify it under the
# terms of the BSD-3 license. We welcome feedback and contributions, see file
# CONTRIBUTING.md for details.

name: "Tests"

on:
  push:
    branches:
      - master
      - next
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  ZIG_VERSION: 0.16.0
  SLANG_VERSION: 2025.23.1

jobs:
  zig-build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: debug-static
            optimize: Debug
            extra_args: ""
            build_targets: true
          - name: release-static
            optimize: ReleaseSafe
            extra_args: ""
            build_targets: true
          - name: release-shared
            optimize: ReleaseSafe
            extra_args: "-Dshared=true"
            build_targets: false
          # - name: release-single-precision
          #   optimize: ReleaseSafe
          #   extra_args: "-Dprecision=single"
          #   build_targets: false

    name: ${{ matrix.name }}

    steps:
      - uses: actions/checkout@v6

      - name: Configure workspace
        run: |
          set -euxo pipefail
          workdir="$RUNNER_TEMP/work-${{ matrix.name }}"
          rm -rf "$workdir"
          mkdir -p "$workdir"
          cp -a "$GITHUB_WORKSPACE"/. "$workdir"/
          echo "MFEM_WORKDIR=$workdir" >> "$GITHUB_ENV"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libvulkan-dev vulkan-tools

      - name: Install Slang compiler
        run: |
          set -euxo pipefail
          base="https://github.com/shader-slang/slang/releases/download/v${SLANG_VERSION}/slang-${SLANG_VERSION}-linux-x86_64.tar.gz"
          mkdir -p "$RUNNER_TEMP/slang"
          curl -fL "${base}" -o "$RUNNER_TEMP/slang/slang.tar.gz"
          tar -xzf "$RUNNER_TEMP/slang/slang.tar.gz" -C "$RUNNER_TEMP/slang"
          slangc_path="$RUNNER_TEMP/slang/bin/slangc"
          if [ ! -x "$slangc_path" ]; then
            echo "slangc not found" >&2
            exit 1
          fi
          chmod +x "$slangc_path"
          echo "$(dirname "$slangc_path")" >> "$GITHUB_PATH"

      - uses: mlugg/setup-zig@v2
        with:
          version: ${{ env.ZIG_VERSION }}
          use-cache: false

      - name: Configure Zig directories
        run: |
          set -euxo pipefail
          build_root="$RUNNER_TEMP/zig-${{ matrix.name }}"
          mkdir -p "$build_root/global-cache"
          mkdir -p "$build_root/global-cache/tmp"
          mkdir -p "$build_root/local-cache"
          mkdir -p "$build_root/local-cache/tmp"
          mkdir -p "$build_root/zig-out"
          {
            echo "ZIG_BUILD_ROOT=$build_root"
            echo "ZIG_GLOBAL_CACHE_DIR=$build_root/global-cache"
            echo "ZIG_LOCAL_CACHE_DIR=$build_root/local-cache"
            echo "ZIG_PREFIX=$build_root/zig-out"
          } >> "$GITHUB_ENV"

      - name: Check build script formatting
        run: |
          cd "$MFEM_WORKDIR"
          zig fmt --check build.zig

      - name: Build
        run: |
          cd "$MFEM_WORKDIR"
          zig build -Doptimize=${{ matrix.optimize }} ${{ matrix.extra_args }} --summary all

      - name: Build Examples
        if: ${{ matrix.build_targets }}
        run: |
          cd "$MFEM_WORKDIR"
          zig build examples -Doptimize=${{ matrix.optimize }} ${{ matrix.extra_args }} --summary all

      - name: Build Miniapps
        if: ${{ matrix.build_targets }}
        run: |
          cd "$MFEM_WORKDIR"
          zig build miniapps -Doptimize=${{ matrix.optimize }} ${{ matrix.extra_args }} --summary all

      - name: Build And Run Tests
        if: ${{ matrix.build_targets }}
        run: |
          cd "$MFEM_WORKDIR"
          zig build test -Doptimize=${{ matrix.optimize }} ${{ matrix.extra_args }} --summary all
